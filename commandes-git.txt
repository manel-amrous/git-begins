commande pour cloner :
git clone  https://liens-vers-le-repository


les trois commandes de push :
 - git add .
 - git commit -m "message"
 - git push -u origin main 

commande pour pull :
git pull origin main

commande pour créer une brache :
 git branche nom-branch

commande pour switcher de branch:
 git switch nom_branch 